@{
    ViewData["Title"] = "Лабораторна #3";
}

<div class="container d-flex justify-content-center">
    <div id="content3" class="container w-50">
        <h4>
            В першому рядку задається кількість прямокутників (n), а в наступних n рядках вводяться координати прямокутників.
            Програма знаходить максимальну площу перетину двох прямокутників і виводить результат.
        </h4>
        <form id="calculationForm" asp-action="Lab3" method="post">
            <div class="form-group row mt-2 mb-2">
                <label for="inputNumber" class="col-form-label">Введіть кількість прямокутників (n):</label>
                <div class="col-sm-10">
                    <input type="number" class="form-control" id="inputNumber" name="n" required>
                </div>
            </div>
            <div class="form-group row mt-2 mb-2">
                <label for="inputRectangles" class="col-form-label">Введіть координати прямокутників (координати через пробіл для кожного прямокутника):</label>
                <div class="col-sm-10">
                    <textarea class="form-control" id="inputRectangles" name="rectangles" rows="5" required></textarea>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-sm-10">
                    <button class="btn btn-primary" type="submit" onclick="submitForm()">Розрахувати</button>
                </div>
            </div>
        </form>

        <div id="resultArea" class="mt-3">
            <label for="result">Результат:</label>
            <span id="result">@ViewBag.Result</span>
        </div>
    </div>
</div>

<script>
    function showTab(tabId, aId) {
        $('.tab-pane').removeClass('show active');
        $('#' + tabId).addClass('show active');
        $('.nav-link').removeClass('active');
        $('#' + aId).addClass('active');
    }

    function submitForm() {
        $('#result').text('');
    }
</script>
