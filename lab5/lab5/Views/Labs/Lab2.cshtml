@{
    ViewData["Title"] = "Лабораторна #2";
}
<div class="container d-flex justify-content-center">
    <div id="content2" class="container w-50">
        <h4>
            Програма виводить кількість ентів, які знають рівно K слів, за модулем P.
        </h4>
        <p>Будь ласка, введіть K та P, щоб програма вивела кількість ентів, які знають рівно K слів, за модулем P.</p>
        <form id="calculationForm" asp-action="Lab2" method="post">
            <div class="form-group row mt-2 mb-2">
                <label for="inputNumber" class="col-form-label">Введіть K:</label>
                <div class="col-sm-10">
                    <input type="number" class="form-control" id="inputNumber" pattern="^\d+$" name="K" required>
                </div>
            </div>
            <div class="form-group row mt-2 mb-2">
                <label for="inputNumbers" class="col-form-label">Введіть P:</label>
                <div class="col-sm-10">
                    <input type="number" class="form-control" id="inputNumbers" pattern="^\d+$" name="P" required>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-sm-10">
                    <button class="btn btn-primary" type="submit" onclick="submitForm()">Розрахувати ентів</button>
                </div>
            </div>
        </form>

        <div id="resultArea" class="mt-3">
            <label for="result">Результат:</label>
            <span id="result">@ViewBag.Result</span>
        </div>
    </div>
</div>

<script>
    function showTab(tabId, aId) {
        $('.tab-pane').removeClass('show active');
        $('#' + tabId).addClass('show active');
        $('.nav-link').removeClass('active');
        $('#' + aId).addClass('active');
    }
    
    function submitForm() {
        $('#result').text('');
    }
</script>